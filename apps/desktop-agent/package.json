{
	"name": "@runa/desktop-agent",
	"version": "0.1.0",
	"private": true,
	"description": "Runa desktop agent - secure desktop bridge foundation with Electron shell",
	"author": "Runa Team",
	"type": "module",
	"main": "electron/main.js",
	"scripts": {
		"build": "node scripts/build-main.mjs",
		"build:renderer": "node scripts/build-renderer.mjs",
		"build:sidecar": "node scripts/build-native-input-go.mjs",
		"dist:win": "npm run build && npm run build:renderer && electron-builder --win --config electron-builder.yml",
		"start:electron": "node scripts/start-electron.mjs",
		"typecheck": "tsc --noEmit",
		"typecheck:electron": "tsc -p tsconfig.electron.json --noEmit",
		"typecheck:renderer": "tsc -p tsconfig.renderer.json --noEmit"
	},
	"dependencies": {
		"@runa/types": "workspace:*",
		"electron-updater": "^6.6.2"
	},
	"devDependencies": {
		"@types/node": "^25.6.0",
		"@types/react": "^19.0.0",
		"@types/react-dom": "^19.0.0",
		"electron": "^33.0.0",
		"electron-builder": "^26.0.12",
		"esbuild": "^0.24.0",
		"react": "^19.0.0",
		"react-dom": "^19.0.0",
		"typescript": "^5.7.0"
	}
}
